---
description: 构建配置与依赖管理
globs: build.gradle.kts, gradle.properties, gradle/libs.versions.toml
alwaysApply: true
---

# 构建配置参考

## 文件职责

| 文件 | 作用 |
|------|------|
| `gradle.properties` | 插件元信息、平台版本、插件依赖 |
| `gradle/libs.versions.toml` | 插件/库版本号集中管理（Gradle 依赖锁定） |
| `build.gradle.kts` | 构建逻辑、自定义任务、依赖声明 |

## gradle.properties 关键配置

```properties
pluginGroup              # Maven group ID
pluginName              # 插件显示名称
pluginVersion           # 语义化版本（如 2.5.0）
pluginSinceBuild         # 最低兼容 IDE build 号（包含）
pluginUntilBuild        # 最高兼容 IDE build 号（支持通配符）
platformType            # IU=IntelliJ IDEA，GO=GoLand，PY=PyCharm 等
platformVersion         # IntelliJ Platform 版本（如 2026.2）
platformPlugins         # Marketplace 插件，格式：插件ID:版本号
platformBundledPlugins  # IntelliJ 内置插件
gradleVersion           # Gradle wrapper 版本
org.gradle.configuration-cache  # 必须设为 false，Foojay toolchain 兼容需要
org.gradle.caching      # 启用构建缓存
```

## gradle/libs.versions.toml 结构

```toml
[versions]       # 所有版本号集中管理
[libraries]      # 第三方库依赖
[plugins]        # Gradle 插件版本（通过 alias 在 build.gradle.kts 中引用）
```

## 添加新插件依赖

1. 在 `gradle.properties` 中添加：
   ```properties
   platformPlugins=已存在的:版本, 新插件ID:版本
   ```

2. 在 `src/main/resources/META-INF/plugin.xml` 中添加可选依赖：
   ```xml
   <depends optional="true" config-file="protobuf-新功能.xml">新插件ID</depends>
   ```

3. 创建 `src/main/resources/META-INF/protobuf-新功能.xml`，编写扩展点

4. 若 Kotlin 代码中使用了该插件提供的 API，必须将其加入 `platformPlugins`

## 版本更新工作流

1. 更新 `gradle/libs.versions.toml` → 执行 `./gradlew build` 检查是否有新版本提示
2. 参考 `intellij-platform-upgrade.mdc` 中的检查清单同步更新所有相关配置
3. 执行 `./gradlew --stop` 后重新构建
4. 确认以下任务全部通过：`generateLexer`、`generateParser`、`compileKotlin`、`build`

## 参考文档

详见中文文档 `docs/getting-started-zh.md`（快速开始）和 `docs/architecture-zh.md`（架构概览）。
