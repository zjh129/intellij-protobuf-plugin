---
description: 扩展点与集成模块开发
globs: src/main/kotlin/**/*.kt, src/main/resources/META-INF/*.xml
alwaysApply: false
---

# 扩展点与集成模块开发

## 五个扩展点

插件定义了 5 个可被第三方插件扩展的扩展点（详见 `docs/extension-points-zh.md`）：

| 扩展点 | 接口 | 功能 |
|--------|------|------|
| `rootProvider` | `ProtobufRootProvider` | 添加自定义 proto 文件搜索位置 |
| `symbolReferenceProvider` | `ProtobufSymbolReferenceProvider` | 添加自定义符号解析策略 |
| `indexProvider` | `ProtobufIndexProvider` | 向 stub 索引贡献额外数据 |
| `stubExternalProvider` | `ProtobufStubExternalProvider` | 向 stub 附加外部元数据 |
| `protocPlugin` | `ProtobufCompilerPlugin` | 扩展内部编译器 |

## 添加新语言功能步骤

1. **注解器 / 检查**：添加到 `lang/annotator/`，在 `plugin.xml` 中注册
2. **补全**：在 `lang/completion/` 添加 provider，在 `ProtobufCompletionContributor` 中注册
3. **快速修复**：添加到 `lang/quickfix/`，通过注解器或检查器注册
4. **引用**：在 `lang/reference/` 添加 provider，在 `ProtobufSymbolReferenceContributor` 中注册

各子系统的设计原理详见 `docs/design/`。

## 添加新集成模块步骤

1. 在 `src/main/kotlin/.../protobuf/<module>/` 下创建包
2. 在 `src/main/resources/META-INF/` 下创建 XML 配置文件（`io.kanro.idea.plugin.protobuf-<module>.xml`）
3. 在主 `plugin.xml` 中添加可选依赖 `<depends optional="true" config-file="protobuf-<module>.xml">`
4. 实现所需的 provider（索引、行标记、查找用法）
5. 在 `docs/modules/<module>-zh.md` 中添加文档

## 注册扩展点的 XML 格式

```xml
<extensions defaultExtensionNs="io.kanro.idea.plugin.protobuf">
    <rootProvider implementation="com.example.MyRootProvider"/>
    <indexProvider implementation="com.example.MyIndexProvider"/>
</extensions>
```

## Copilot AI 开发工作流

本项目使用 AI 辅助开发，遵循结构化周期：

```
brainstorm → implement → ship → reflect
```

- **Brainstorm**：设计讨论在 `.github/brainstorm.md` 中进行，带状态机防止过早编码
- **Ship**：提交经过构建验证、测试执行和 brainstorm 状态检查
- **Reflect**：发版后反思在学习库 `.github/knowledge.md` 中积累经验

详见 `.github/copilot-instructions.md`。
